import 'package:flutter/material.dart';

class PlacesDetatilScreen extends StatelessWidget {
  const PlacesDetatilScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
